import React from 'react'
import styled from 'styled-components'

const Recommend = (props) => {
  return <Container id={props.id}>ddd</Container>
}

const Container = styled.div`
  display: flex;
`

export default Recommend
